fun gcd(var x: Int) -> (var y: Int) -> Int {
    fun _gcd(var y: Int) -> Int {
        if y == 0 {
            return x
        }
        return gcd(x = y)(y = x % y)
    }
}
var a: Int
a = gcd(x = 12)(y = 8)
